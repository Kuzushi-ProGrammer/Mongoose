using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestVar : MonoBehaviour
{

    public static string splatoon = "squid";

    // public makes the variable visible
    // static makes the variable the same across all instances of the object with the script on it

}
